---
description:
globs:
alwaysApply: false
---

수정파일경로) app/backend


요구사항 데이터베이스를 사용하지말것.
데이터베이스와 통신하는 관련된 코드를 삭제할것.
삭제된 코드의 주요 기능을 체크리스트로 반환할것.
유저의 정보를 서버에 저장하지말것.

데이터베이스와 통신하는 코드가 사라지면서 발생하는 에러가 생긴다면 대체 할수잇는 코드를 작성하여 기능이 정상작동하도록 할것.

추가된 코드를 체크리스트로 반환할것.

지금까지 작성된 코드스타일을 참고하여 구현할것.
현재사용하지 않거나 불필요한 코드와 파일을 삭제할것.
중복되는 코드가 있다면 중복된 코드를 합칠것.
또한 중복적으로 사용되는 변수가 객체가 있다면 전역상수로 만들어 import 하여 사용할것.

사용하지않거나 불필요한 패키지를 삭제할것.

최종적으로 직접 bulid 하여 발생하는 오류를 확인하고 해결할것.
어떠한 방식으로 해결하였는지 체크리스트로 반환할것.
lint 에러 확인하여 수정할것.


backend 폴더안에 있는 파일외엔 수정하지말것.