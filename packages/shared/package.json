{
  "name": "@keyword-selector/shared",
  "version": "0.1.0",
  "description": "키워드 셀렉터 공통 타입 및 유틸리티",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "class-validator": "^0.14.0",
    "class-transformer": "^0.5.1"
  }
}
